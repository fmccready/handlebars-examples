<div class="container">
    <h1>{{title}}</h1>
    <div class="body">
        {{body}}
    </div>
    <h2>List</h2>
    <ul>
        {{#each list}}
            <li class="{{class}}"><button class="btn btn-primary">{{text}}</button></li>
        {{/each}}
    </ul>
</div>